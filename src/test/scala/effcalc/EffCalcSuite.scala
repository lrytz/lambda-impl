package effcalc

import org.scalatest.FunSuite
import org.junit.runner.RunWith
import org.scalatest.junit.JUnitRunner

@RunWith(classOf[JUnitRunner])
class EffCalcSuite extends FunSuite {
  test("nil") {
    assert(1 === 1)
  }
}