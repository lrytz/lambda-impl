let e1: Nat =>[e1] Nat = (x: Nat) => succ x in
let e2: Nat =>[e2] Nat = (x: Nat) => succ x in

let etop: Nat => Nat = (x: Nat) => x in
let id: Nat =>[bot] Nat = (x: Nat) => x in

let app1: (Nat => Nat) -> Nat = (f: Nat => Nat) -> f 1 in

id (e1 (app1 e2))
